/*
 * This placeholder is replaced at GitHub Pages build time with the public
 * `LOTW_SYNC_ENDPOINT` GitHub Actions variable. It is not a secret.
 * For local development, replace it with your deployed Worker /sync URL.
 */
window.BETTERLOTW_CONFIG = { syncEndpoint: "__LOTW_SYNC_ENDPOINT__" };
